// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/control-plane/ent/predicate"
	"opl-cloud/services/control-plane/ent/workspacebackup"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WorkspaceBackupQuery is the builder for querying WorkspaceBackup entities.
type WorkspaceBackupQuery struct {
	config
	ctx        *QueryContext
	order      []workspacebackup.OrderOption
	inters     []Interceptor
	predicates []predicate.WorkspaceBackup
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WorkspaceBackupQuery builder.
func (wbq *WorkspaceBackupQuery) Where(ps ...predicate.WorkspaceBackup) *WorkspaceBackupQuery {
	wbq.predicates = append(wbq.predicates, ps...)
	return wbq
}

// Limit the number of records to be returned by this query.
func (wbq *WorkspaceBackupQuery) Limit(limit int) *WorkspaceBackupQuery {
	wbq.ctx.Limit = &limit
	return wbq
}

// Offset to start from.
func (wbq *WorkspaceBackupQuery) Offset(offset int) *WorkspaceBackupQuery {
	wbq.ctx.Offset = &offset
	return wbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wbq *WorkspaceBackupQuery) Unique(unique bool) *WorkspaceBackupQuery {
	wbq.ctx.Unique = &unique
	return wbq
}

// Order specifies how the records should be ordered.
func (wbq *WorkspaceBackupQuery) Order(o ...workspacebackup.OrderOption) *WorkspaceBackupQuery {
	wbq.order = append(wbq.order, o...)
	return wbq
}

// First returns the first WorkspaceBackup entity from the query.
// Returns a *NotFoundError when no WorkspaceBackup was found.
func (wbq *WorkspaceBackupQuery) First(ctx context.Context) (*WorkspaceBackup, error) {
	nodes, err := wbq.Limit(1).All(setContextOp(ctx, wbq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{workspacebackup.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wbq *WorkspaceBackupQuery) FirstX(ctx context.Context) *WorkspaceBackup {
	node, err := wbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WorkspaceBackup ID from the query.
// Returns a *NotFoundError when no WorkspaceBackup ID was found.
func (wbq *WorkspaceBackupQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = wbq.Limit(1).IDs(setContextOp(ctx, wbq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{workspacebackup.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wbq *WorkspaceBackupQuery) FirstIDX(ctx context.Context) string {
	id, err := wbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WorkspaceBackup entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WorkspaceBackup entity is found.
// Returns a *NotFoundError when no WorkspaceBackup entities are found.
func (wbq *WorkspaceBackupQuery) Only(ctx context.Context) (*WorkspaceBackup, error) {
	nodes, err := wbq.Limit(2).All(setContextOp(ctx, wbq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{workspacebackup.Label}
	default:
		return nil, &NotSingularError{workspacebackup.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wbq *WorkspaceBackupQuery) OnlyX(ctx context.Context) *WorkspaceBackup {
	node, err := wbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WorkspaceBackup ID in the query.
// Returns a *NotSingularError when more than one WorkspaceBackup ID is found.
// Returns a *NotFoundError when no entities are found.
func (wbq *WorkspaceBackupQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = wbq.Limit(2).IDs(setContextOp(ctx, wbq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{workspacebackup.Label}
	default:
		err = &NotSingularError{workspacebackup.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wbq *WorkspaceBackupQuery) OnlyIDX(ctx context.Context) string {
	id, err := wbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WorkspaceBackups.
func (wbq *WorkspaceBackupQuery) All(ctx context.Context) ([]*WorkspaceBackup, error) {
	ctx = setContextOp(ctx, wbq.ctx, "All")
	if err := wbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WorkspaceBackup, *WorkspaceBackupQuery]()
	return withInterceptors[[]*WorkspaceBackup](ctx, wbq, qr, wbq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wbq *WorkspaceBackupQuery) AllX(ctx context.Context) []*WorkspaceBackup {
	nodes, err := wbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WorkspaceBackup IDs.
func (wbq *WorkspaceBackupQuery) IDs(ctx context.Context) (ids []string, err error) {
	if wbq.ctx.Unique == nil && wbq.path != nil {
		wbq.Unique(true)
	}
	ctx = setContextOp(ctx, wbq.ctx, "IDs")
	if err = wbq.Select(workspacebackup.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wbq *WorkspaceBackupQuery) IDsX(ctx context.Context) []string {
	ids, err := wbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wbq *WorkspaceBackupQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wbq.ctx, "Count")
	if err := wbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wbq, querierCount[*WorkspaceBackupQuery](), wbq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wbq *WorkspaceBackupQuery) CountX(ctx context.Context) int {
	count, err := wbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wbq *WorkspaceBackupQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wbq.ctx, "Exist")
	switch _, err := wbq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wbq *WorkspaceBackupQuery) ExistX(ctx context.Context) bool {
	exist, err := wbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WorkspaceBackupQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wbq *WorkspaceBackupQuery) Clone() *WorkspaceBackupQuery {
	if wbq == nil {
		return nil
	}
	return &WorkspaceBackupQuery{
		config:     wbq.config,
		ctx:        wbq.ctx.Clone(),
		order:      append([]workspacebackup.OrderOption{}, wbq.order...),
		inters:     append([]Interceptor{}, wbq.inters...),
		predicates: append([]predicate.WorkspaceBackup{}, wbq.predicates...),
		// clone intermediate query.
		sql:  wbq.sql.Clone(),
		path: wbq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WorkspaceBackup.Query().
//		GroupBy(workspacebackup.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wbq *WorkspaceBackupQuery) GroupBy(field string, fields ...string) *WorkspaceBackupGroupBy {
	wbq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WorkspaceBackupGroupBy{build: wbq}
	grbuild.flds = &wbq.ctx.Fields
	grbuild.label = workspacebackup.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.WorkspaceBackup.Query().
//		Select(workspacebackup.FieldCreatedAt).
//		Scan(ctx, &v)
func (wbq *WorkspaceBackupQuery) Select(fields ...string) *WorkspaceBackupSelect {
	wbq.ctx.Fields = append(wbq.ctx.Fields, fields...)
	sbuild := &WorkspaceBackupSelect{WorkspaceBackupQuery: wbq}
	sbuild.label = workspacebackup.Label
	sbuild.flds, sbuild.scan = &wbq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WorkspaceBackupSelect configured with the given aggregations.
func (wbq *WorkspaceBackupQuery) Aggregate(fns ...AggregateFunc) *WorkspaceBackupSelect {
	return wbq.Select().Aggregate(fns...)
}

func (wbq *WorkspaceBackupQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wbq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wbq); err != nil {
				return err
			}
		}
	}
	for _, f := range wbq.ctx.Fields {
		if !workspacebackup.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wbq.path != nil {
		prev, err := wbq.path(ctx)
		if err != nil {
			return err
		}
		wbq.sql = prev
	}
	return nil
}

func (wbq *WorkspaceBackupQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WorkspaceBackup, error) {
	var (
		nodes = []*WorkspaceBackup{}
		_spec = wbq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WorkspaceBackup).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WorkspaceBackup{config: wbq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wbq *WorkspaceBackupQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wbq.querySpec()
	_spec.Node.Columns = wbq.ctx.Fields
	if len(wbq.ctx.Fields) > 0 {
		_spec.Unique = wbq.ctx.Unique != nil && *wbq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wbq.driver, _spec)
}

func (wbq *WorkspaceBackupQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(workspacebackup.Table, workspacebackup.Columns, sqlgraph.NewFieldSpec(workspacebackup.FieldID, field.TypeString))
	_spec.From = wbq.sql
	if unique := wbq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wbq.path != nil {
		_spec.Unique = true
	}
	if fields := wbq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, workspacebackup.FieldID)
		for i := range fields {
			if fields[i] != workspacebackup.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wbq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wbq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wbq *WorkspaceBackupQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wbq.driver.Dialect())
	t1 := builder.Table(workspacebackup.Table)
	columns := wbq.ctx.Fields
	if len(columns) == 0 {
		columns = workspacebackup.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wbq.sql != nil {
		selector = wbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wbq.ctx.Unique != nil && *wbq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wbq.predicates {
		p(selector)
	}
	for _, p := range wbq.order {
		p(selector)
	}
	if offset := wbq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wbq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WorkspaceBackupGroupBy is the group-by builder for WorkspaceBackup entities.
type WorkspaceBackupGroupBy struct {
	selector
	build *WorkspaceBackupQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wbgb *WorkspaceBackupGroupBy) Aggregate(fns ...AggregateFunc) *WorkspaceBackupGroupBy {
	wbgb.fns = append(wbgb.fns, fns...)
	return wbgb
}

// Scan applies the selector query and scans the result into the given value.
func (wbgb *WorkspaceBackupGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wbgb.build.ctx, "GroupBy")
	if err := wbgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkspaceBackupQuery, *WorkspaceBackupGroupBy](ctx, wbgb.build, wbgb, wbgb.build.inters, v)
}

func (wbgb *WorkspaceBackupGroupBy) sqlScan(ctx context.Context, root *WorkspaceBackupQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wbgb.fns))
	for _, fn := range wbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wbgb.flds)+len(wbgb.fns))
		for _, f := range *wbgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wbgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wbgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WorkspaceBackupSelect is the builder for selecting fields of WorkspaceBackup entities.
type WorkspaceBackupSelect struct {
	*WorkspaceBackupQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wbs *WorkspaceBackupSelect) Aggregate(fns ...AggregateFunc) *WorkspaceBackupSelect {
	wbs.fns = append(wbs.fns, fns...)
	return wbs
}

// Scan applies the selector query and scans the result into the given value.
func (wbs *WorkspaceBackupSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wbs.ctx, "Select")
	if err := wbs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkspaceBackupQuery, *WorkspaceBackupSelect](ctx, wbs.WorkspaceBackupQuery, wbs, wbs.inters, v)
}

func (wbs *WorkspaceBackupSelect) sqlScan(ctx context.Context, root *WorkspaceBackupQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wbs.fns))
	for _, fn := range wbs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wbs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
