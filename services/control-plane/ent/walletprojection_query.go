// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/control-plane/ent/predicate"
	"opl-cloud/services/control-plane/ent/walletprojection"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WalletProjectionQuery is the builder for querying WalletProjection entities.
type WalletProjectionQuery struct {
	config
	ctx        *QueryContext
	order      []walletprojection.OrderOption
	inters     []Interceptor
	predicates []predicate.WalletProjection
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WalletProjectionQuery builder.
func (wpq *WalletProjectionQuery) Where(ps ...predicate.WalletProjection) *WalletProjectionQuery {
	wpq.predicates = append(wpq.predicates, ps...)
	return wpq
}

// Limit the number of records to be returned by this query.
func (wpq *WalletProjectionQuery) Limit(limit int) *WalletProjectionQuery {
	wpq.ctx.Limit = &limit
	return wpq
}

// Offset to start from.
func (wpq *WalletProjectionQuery) Offset(offset int) *WalletProjectionQuery {
	wpq.ctx.Offset = &offset
	return wpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wpq *WalletProjectionQuery) Unique(unique bool) *WalletProjectionQuery {
	wpq.ctx.Unique = &unique
	return wpq
}

// Order specifies how the records should be ordered.
func (wpq *WalletProjectionQuery) Order(o ...walletprojection.OrderOption) *WalletProjectionQuery {
	wpq.order = append(wpq.order, o...)
	return wpq
}

// First returns the first WalletProjection entity from the query.
// Returns a *NotFoundError when no WalletProjection was found.
func (wpq *WalletProjectionQuery) First(ctx context.Context) (*WalletProjection, error) {
	nodes, err := wpq.Limit(1).All(setContextOp(ctx, wpq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{walletprojection.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wpq *WalletProjectionQuery) FirstX(ctx context.Context) *WalletProjection {
	node, err := wpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WalletProjection ID from the query.
// Returns a *NotFoundError when no WalletProjection ID was found.
func (wpq *WalletProjectionQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = wpq.Limit(1).IDs(setContextOp(ctx, wpq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{walletprojection.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wpq *WalletProjectionQuery) FirstIDX(ctx context.Context) string {
	id, err := wpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WalletProjection entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WalletProjection entity is found.
// Returns a *NotFoundError when no WalletProjection entities are found.
func (wpq *WalletProjectionQuery) Only(ctx context.Context) (*WalletProjection, error) {
	nodes, err := wpq.Limit(2).All(setContextOp(ctx, wpq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{walletprojection.Label}
	default:
		return nil, &NotSingularError{walletprojection.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wpq *WalletProjectionQuery) OnlyX(ctx context.Context) *WalletProjection {
	node, err := wpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WalletProjection ID in the query.
// Returns a *NotSingularError when more than one WalletProjection ID is found.
// Returns a *NotFoundError when no entities are found.
func (wpq *WalletProjectionQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = wpq.Limit(2).IDs(setContextOp(ctx, wpq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{walletprojection.Label}
	default:
		err = &NotSingularError{walletprojection.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wpq *WalletProjectionQuery) OnlyIDX(ctx context.Context) string {
	id, err := wpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WalletProjections.
func (wpq *WalletProjectionQuery) All(ctx context.Context) ([]*WalletProjection, error) {
	ctx = setContextOp(ctx, wpq.ctx, "All")
	if err := wpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WalletProjection, *WalletProjectionQuery]()
	return withInterceptors[[]*WalletProjection](ctx, wpq, qr, wpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wpq *WalletProjectionQuery) AllX(ctx context.Context) []*WalletProjection {
	nodes, err := wpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WalletProjection IDs.
func (wpq *WalletProjectionQuery) IDs(ctx context.Context) (ids []string, err error) {
	if wpq.ctx.Unique == nil && wpq.path != nil {
		wpq.Unique(true)
	}
	ctx = setContextOp(ctx, wpq.ctx, "IDs")
	if err = wpq.Select(walletprojection.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wpq *WalletProjectionQuery) IDsX(ctx context.Context) []string {
	ids, err := wpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wpq *WalletProjectionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wpq.ctx, "Count")
	if err := wpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wpq, querierCount[*WalletProjectionQuery](), wpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wpq *WalletProjectionQuery) CountX(ctx context.Context) int {
	count, err := wpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wpq *WalletProjectionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wpq.ctx, "Exist")
	switch _, err := wpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wpq *WalletProjectionQuery) ExistX(ctx context.Context) bool {
	exist, err := wpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WalletProjectionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wpq *WalletProjectionQuery) Clone() *WalletProjectionQuery {
	if wpq == nil {
		return nil
	}
	return &WalletProjectionQuery{
		config:     wpq.config,
		ctx:        wpq.ctx.Clone(),
		order:      append([]walletprojection.OrderOption{}, wpq.order...),
		inters:     append([]Interceptor{}, wpq.inters...),
		predicates: append([]predicate.WalletProjection{}, wpq.predicates...),
		// clone intermediate query.
		sql:  wpq.sql.Clone(),
		path: wpq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WalletProjection.Query().
//		GroupBy(walletprojection.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wpq *WalletProjectionQuery) GroupBy(field string, fields ...string) *WalletProjectionGroupBy {
	wpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WalletProjectionGroupBy{build: wpq}
	grbuild.flds = &wpq.ctx.Fields
	grbuild.label = walletprojection.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.WalletProjection.Query().
//		Select(walletprojection.FieldCreatedAt).
//		Scan(ctx, &v)
func (wpq *WalletProjectionQuery) Select(fields ...string) *WalletProjectionSelect {
	wpq.ctx.Fields = append(wpq.ctx.Fields, fields...)
	sbuild := &WalletProjectionSelect{WalletProjectionQuery: wpq}
	sbuild.label = walletprojection.Label
	sbuild.flds, sbuild.scan = &wpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WalletProjectionSelect configured with the given aggregations.
func (wpq *WalletProjectionQuery) Aggregate(fns ...AggregateFunc) *WalletProjectionSelect {
	return wpq.Select().Aggregate(fns...)
}

func (wpq *WalletProjectionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wpq); err != nil {
				return err
			}
		}
	}
	for _, f := range wpq.ctx.Fields {
		if !walletprojection.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wpq.path != nil {
		prev, err := wpq.path(ctx)
		if err != nil {
			return err
		}
		wpq.sql = prev
	}
	return nil
}

func (wpq *WalletProjectionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WalletProjection, error) {
	var (
		nodes = []*WalletProjection{}
		_spec = wpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WalletProjection).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WalletProjection{config: wpq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wpq *WalletProjectionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wpq.querySpec()
	_spec.Node.Columns = wpq.ctx.Fields
	if len(wpq.ctx.Fields) > 0 {
		_spec.Unique = wpq.ctx.Unique != nil && *wpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wpq.driver, _spec)
}

func (wpq *WalletProjectionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(walletprojection.Table, walletprojection.Columns, sqlgraph.NewFieldSpec(walletprojection.FieldID, field.TypeString))
	_spec.From = wpq.sql
	if unique := wpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wpq.path != nil {
		_spec.Unique = true
	}
	if fields := wpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, walletprojection.FieldID)
		for i := range fields {
			if fields[i] != walletprojection.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wpq *WalletProjectionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wpq.driver.Dialect())
	t1 := builder.Table(walletprojection.Table)
	columns := wpq.ctx.Fields
	if len(columns) == 0 {
		columns = walletprojection.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wpq.sql != nil {
		selector = wpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wpq.ctx.Unique != nil && *wpq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wpq.predicates {
		p(selector)
	}
	for _, p := range wpq.order {
		p(selector)
	}
	if offset := wpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WalletProjectionGroupBy is the group-by builder for WalletProjection entities.
type WalletProjectionGroupBy struct {
	selector
	build *WalletProjectionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wpgb *WalletProjectionGroupBy) Aggregate(fns ...AggregateFunc) *WalletProjectionGroupBy {
	wpgb.fns = append(wpgb.fns, fns...)
	return wpgb
}

// Scan applies the selector query and scans the result into the given value.
func (wpgb *WalletProjectionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wpgb.build.ctx, "GroupBy")
	if err := wpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WalletProjectionQuery, *WalletProjectionGroupBy](ctx, wpgb.build, wpgb, wpgb.build.inters, v)
}

func (wpgb *WalletProjectionGroupBy) sqlScan(ctx context.Context, root *WalletProjectionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wpgb.fns))
	for _, fn := range wpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wpgb.flds)+len(wpgb.fns))
		for _, f := range *wpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WalletProjectionSelect is the builder for selecting fields of WalletProjection entities.
type WalletProjectionSelect struct {
	*WalletProjectionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wps *WalletProjectionSelect) Aggregate(fns ...AggregateFunc) *WalletProjectionSelect {
	wps.fns = append(wps.fns, fns...)
	return wps
}

// Scan applies the selector query and scans the result into the given value.
func (wps *WalletProjectionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wps.ctx, "Select")
	if err := wps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WalletProjectionQuery, *WalletProjectionSelect](ctx, wps.WalletProjectionQuery, wps, wps.inters, v)
}

func (wps *WalletProjectionSelect) sqlScan(ctx context.Context, root *WalletProjectionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wps.fns))
	for _, fn := range wps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
