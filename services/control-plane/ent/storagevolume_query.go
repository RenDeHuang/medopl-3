// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/control-plane/ent/predicate"
	"opl-cloud/services/control-plane/ent/storagevolume"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// StorageVolumeQuery is the builder for querying StorageVolume entities.
type StorageVolumeQuery struct {
	config
	ctx        *QueryContext
	order      []storagevolume.OrderOption
	inters     []Interceptor
	predicates []predicate.StorageVolume
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StorageVolumeQuery builder.
func (svq *StorageVolumeQuery) Where(ps ...predicate.StorageVolume) *StorageVolumeQuery {
	svq.predicates = append(svq.predicates, ps...)
	return svq
}

// Limit the number of records to be returned by this query.
func (svq *StorageVolumeQuery) Limit(limit int) *StorageVolumeQuery {
	svq.ctx.Limit = &limit
	return svq
}

// Offset to start from.
func (svq *StorageVolumeQuery) Offset(offset int) *StorageVolumeQuery {
	svq.ctx.Offset = &offset
	return svq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (svq *StorageVolumeQuery) Unique(unique bool) *StorageVolumeQuery {
	svq.ctx.Unique = &unique
	return svq
}

// Order specifies how the records should be ordered.
func (svq *StorageVolumeQuery) Order(o ...storagevolume.OrderOption) *StorageVolumeQuery {
	svq.order = append(svq.order, o...)
	return svq
}

// First returns the first StorageVolume entity from the query.
// Returns a *NotFoundError when no StorageVolume was found.
func (svq *StorageVolumeQuery) First(ctx context.Context) (*StorageVolume, error) {
	nodes, err := svq.Limit(1).All(setContextOp(ctx, svq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{storagevolume.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (svq *StorageVolumeQuery) FirstX(ctx context.Context) *StorageVolume {
	node, err := svq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StorageVolume ID from the query.
// Returns a *NotFoundError when no StorageVolume ID was found.
func (svq *StorageVolumeQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = svq.Limit(1).IDs(setContextOp(ctx, svq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{storagevolume.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (svq *StorageVolumeQuery) FirstIDX(ctx context.Context) string {
	id, err := svq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StorageVolume entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StorageVolume entity is found.
// Returns a *NotFoundError when no StorageVolume entities are found.
func (svq *StorageVolumeQuery) Only(ctx context.Context) (*StorageVolume, error) {
	nodes, err := svq.Limit(2).All(setContextOp(ctx, svq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{storagevolume.Label}
	default:
		return nil, &NotSingularError{storagevolume.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (svq *StorageVolumeQuery) OnlyX(ctx context.Context) *StorageVolume {
	node, err := svq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StorageVolume ID in the query.
// Returns a *NotSingularError when more than one StorageVolume ID is found.
// Returns a *NotFoundError when no entities are found.
func (svq *StorageVolumeQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = svq.Limit(2).IDs(setContextOp(ctx, svq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{storagevolume.Label}
	default:
		err = &NotSingularError{storagevolume.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (svq *StorageVolumeQuery) OnlyIDX(ctx context.Context) string {
	id, err := svq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StorageVolumes.
func (svq *StorageVolumeQuery) All(ctx context.Context) ([]*StorageVolume, error) {
	ctx = setContextOp(ctx, svq.ctx, "All")
	if err := svq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StorageVolume, *StorageVolumeQuery]()
	return withInterceptors[[]*StorageVolume](ctx, svq, qr, svq.inters)
}

// AllX is like All, but panics if an error occurs.
func (svq *StorageVolumeQuery) AllX(ctx context.Context) []*StorageVolume {
	nodes, err := svq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StorageVolume IDs.
func (svq *StorageVolumeQuery) IDs(ctx context.Context) (ids []string, err error) {
	if svq.ctx.Unique == nil && svq.path != nil {
		svq.Unique(true)
	}
	ctx = setContextOp(ctx, svq.ctx, "IDs")
	if err = svq.Select(storagevolume.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (svq *StorageVolumeQuery) IDsX(ctx context.Context) []string {
	ids, err := svq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (svq *StorageVolumeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, svq.ctx, "Count")
	if err := svq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, svq, querierCount[*StorageVolumeQuery](), svq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (svq *StorageVolumeQuery) CountX(ctx context.Context) int {
	count, err := svq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (svq *StorageVolumeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, svq.ctx, "Exist")
	switch _, err := svq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (svq *StorageVolumeQuery) ExistX(ctx context.Context) bool {
	exist, err := svq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StorageVolumeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (svq *StorageVolumeQuery) Clone() *StorageVolumeQuery {
	if svq == nil {
		return nil
	}
	return &StorageVolumeQuery{
		config:     svq.config,
		ctx:        svq.ctx.Clone(),
		order:      append([]storagevolume.OrderOption{}, svq.order...),
		inters:     append([]Interceptor{}, svq.inters...),
		predicates: append([]predicate.StorageVolume{}, svq.predicates...),
		// clone intermediate query.
		sql:  svq.sql.Clone(),
		path: svq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StorageVolume.Query().
//		GroupBy(storagevolume.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (svq *StorageVolumeQuery) GroupBy(field string, fields ...string) *StorageVolumeGroupBy {
	svq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StorageVolumeGroupBy{build: svq}
	grbuild.flds = &svq.ctx.Fields
	grbuild.label = storagevolume.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.StorageVolume.Query().
//		Select(storagevolume.FieldCreatedAt).
//		Scan(ctx, &v)
func (svq *StorageVolumeQuery) Select(fields ...string) *StorageVolumeSelect {
	svq.ctx.Fields = append(svq.ctx.Fields, fields...)
	sbuild := &StorageVolumeSelect{StorageVolumeQuery: svq}
	sbuild.label = storagevolume.Label
	sbuild.flds, sbuild.scan = &svq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StorageVolumeSelect configured with the given aggregations.
func (svq *StorageVolumeQuery) Aggregate(fns ...AggregateFunc) *StorageVolumeSelect {
	return svq.Select().Aggregate(fns...)
}

func (svq *StorageVolumeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range svq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, svq); err != nil {
				return err
			}
		}
	}
	for _, f := range svq.ctx.Fields {
		if !storagevolume.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if svq.path != nil {
		prev, err := svq.path(ctx)
		if err != nil {
			return err
		}
		svq.sql = prev
	}
	return nil
}

func (svq *StorageVolumeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StorageVolume, error) {
	var (
		nodes = []*StorageVolume{}
		_spec = svq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StorageVolume).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StorageVolume{config: svq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, svq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (svq *StorageVolumeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := svq.querySpec()
	_spec.Node.Columns = svq.ctx.Fields
	if len(svq.ctx.Fields) > 0 {
		_spec.Unique = svq.ctx.Unique != nil && *svq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, svq.driver, _spec)
}

func (svq *StorageVolumeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(storagevolume.Table, storagevolume.Columns, sqlgraph.NewFieldSpec(storagevolume.FieldID, field.TypeString))
	_spec.From = svq.sql
	if unique := svq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if svq.path != nil {
		_spec.Unique = true
	}
	if fields := svq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, storagevolume.FieldID)
		for i := range fields {
			if fields[i] != storagevolume.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := svq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := svq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := svq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := svq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (svq *StorageVolumeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(svq.driver.Dialect())
	t1 := builder.Table(storagevolume.Table)
	columns := svq.ctx.Fields
	if len(columns) == 0 {
		columns = storagevolume.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if svq.sql != nil {
		selector = svq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if svq.ctx.Unique != nil && *svq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range svq.predicates {
		p(selector)
	}
	for _, p := range svq.order {
		p(selector)
	}
	if offset := svq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := svq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// StorageVolumeGroupBy is the group-by builder for StorageVolume entities.
type StorageVolumeGroupBy struct {
	selector
	build *StorageVolumeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (svgb *StorageVolumeGroupBy) Aggregate(fns ...AggregateFunc) *StorageVolumeGroupBy {
	svgb.fns = append(svgb.fns, fns...)
	return svgb
}

// Scan applies the selector query and scans the result into the given value.
func (svgb *StorageVolumeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, svgb.build.ctx, "GroupBy")
	if err := svgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StorageVolumeQuery, *StorageVolumeGroupBy](ctx, svgb.build, svgb, svgb.build.inters, v)
}

func (svgb *StorageVolumeGroupBy) sqlScan(ctx context.Context, root *StorageVolumeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(svgb.fns))
	for _, fn := range svgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*svgb.flds)+len(svgb.fns))
		for _, f := range *svgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*svgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := svgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StorageVolumeSelect is the builder for selecting fields of StorageVolume entities.
type StorageVolumeSelect struct {
	*StorageVolumeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (svs *StorageVolumeSelect) Aggregate(fns ...AggregateFunc) *StorageVolumeSelect {
	svs.fns = append(svs.fns, fns...)
	return svs
}

// Scan applies the selector query and scans the result into the given value.
func (svs *StorageVolumeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, svs.ctx, "Select")
	if err := svs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StorageVolumeQuery, *StorageVolumeSelect](ctx, svs.StorageVolumeQuery, svs, svs.inters, v)
}

func (svs *StorageVolumeSelect) sqlScan(ctx context.Context, root *StorageVolumeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(svs.fns))
	for _, fn := range svs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*svs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := svs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
