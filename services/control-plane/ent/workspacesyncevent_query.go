// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/control-plane/ent/predicate"
	"opl-cloud/services/control-plane/ent/workspacesyncevent"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WorkspaceSyncEventQuery is the builder for querying WorkspaceSyncEvent entities.
type WorkspaceSyncEventQuery struct {
	config
	ctx        *QueryContext
	order      []workspacesyncevent.OrderOption
	inters     []Interceptor
	predicates []predicate.WorkspaceSyncEvent
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WorkspaceSyncEventQuery builder.
func (wseq *WorkspaceSyncEventQuery) Where(ps ...predicate.WorkspaceSyncEvent) *WorkspaceSyncEventQuery {
	wseq.predicates = append(wseq.predicates, ps...)
	return wseq
}

// Limit the number of records to be returned by this query.
func (wseq *WorkspaceSyncEventQuery) Limit(limit int) *WorkspaceSyncEventQuery {
	wseq.ctx.Limit = &limit
	return wseq
}

// Offset to start from.
func (wseq *WorkspaceSyncEventQuery) Offset(offset int) *WorkspaceSyncEventQuery {
	wseq.ctx.Offset = &offset
	return wseq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wseq *WorkspaceSyncEventQuery) Unique(unique bool) *WorkspaceSyncEventQuery {
	wseq.ctx.Unique = &unique
	return wseq
}

// Order specifies how the records should be ordered.
func (wseq *WorkspaceSyncEventQuery) Order(o ...workspacesyncevent.OrderOption) *WorkspaceSyncEventQuery {
	wseq.order = append(wseq.order, o...)
	return wseq
}

// First returns the first WorkspaceSyncEvent entity from the query.
// Returns a *NotFoundError when no WorkspaceSyncEvent was found.
func (wseq *WorkspaceSyncEventQuery) First(ctx context.Context) (*WorkspaceSyncEvent, error) {
	nodes, err := wseq.Limit(1).All(setContextOp(ctx, wseq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{workspacesyncevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wseq *WorkspaceSyncEventQuery) FirstX(ctx context.Context) *WorkspaceSyncEvent {
	node, err := wseq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WorkspaceSyncEvent ID from the query.
// Returns a *NotFoundError when no WorkspaceSyncEvent ID was found.
func (wseq *WorkspaceSyncEventQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = wseq.Limit(1).IDs(setContextOp(ctx, wseq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{workspacesyncevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wseq *WorkspaceSyncEventQuery) FirstIDX(ctx context.Context) string {
	id, err := wseq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WorkspaceSyncEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WorkspaceSyncEvent entity is found.
// Returns a *NotFoundError when no WorkspaceSyncEvent entities are found.
func (wseq *WorkspaceSyncEventQuery) Only(ctx context.Context) (*WorkspaceSyncEvent, error) {
	nodes, err := wseq.Limit(2).All(setContextOp(ctx, wseq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{workspacesyncevent.Label}
	default:
		return nil, &NotSingularError{workspacesyncevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wseq *WorkspaceSyncEventQuery) OnlyX(ctx context.Context) *WorkspaceSyncEvent {
	node, err := wseq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WorkspaceSyncEvent ID in the query.
// Returns a *NotSingularError when more than one WorkspaceSyncEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (wseq *WorkspaceSyncEventQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = wseq.Limit(2).IDs(setContextOp(ctx, wseq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{workspacesyncevent.Label}
	default:
		err = &NotSingularError{workspacesyncevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wseq *WorkspaceSyncEventQuery) OnlyIDX(ctx context.Context) string {
	id, err := wseq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WorkspaceSyncEvents.
func (wseq *WorkspaceSyncEventQuery) All(ctx context.Context) ([]*WorkspaceSyncEvent, error) {
	ctx = setContextOp(ctx, wseq.ctx, "All")
	if err := wseq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WorkspaceSyncEvent, *WorkspaceSyncEventQuery]()
	return withInterceptors[[]*WorkspaceSyncEvent](ctx, wseq, qr, wseq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wseq *WorkspaceSyncEventQuery) AllX(ctx context.Context) []*WorkspaceSyncEvent {
	nodes, err := wseq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WorkspaceSyncEvent IDs.
func (wseq *WorkspaceSyncEventQuery) IDs(ctx context.Context) (ids []string, err error) {
	if wseq.ctx.Unique == nil && wseq.path != nil {
		wseq.Unique(true)
	}
	ctx = setContextOp(ctx, wseq.ctx, "IDs")
	if err = wseq.Select(workspacesyncevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wseq *WorkspaceSyncEventQuery) IDsX(ctx context.Context) []string {
	ids, err := wseq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wseq *WorkspaceSyncEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wseq.ctx, "Count")
	if err := wseq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wseq, querierCount[*WorkspaceSyncEventQuery](), wseq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wseq *WorkspaceSyncEventQuery) CountX(ctx context.Context) int {
	count, err := wseq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wseq *WorkspaceSyncEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wseq.ctx, "Exist")
	switch _, err := wseq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wseq *WorkspaceSyncEventQuery) ExistX(ctx context.Context) bool {
	exist, err := wseq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WorkspaceSyncEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wseq *WorkspaceSyncEventQuery) Clone() *WorkspaceSyncEventQuery {
	if wseq == nil {
		return nil
	}
	return &WorkspaceSyncEventQuery{
		config:     wseq.config,
		ctx:        wseq.ctx.Clone(),
		order:      append([]workspacesyncevent.OrderOption{}, wseq.order...),
		inters:     append([]Interceptor{}, wseq.inters...),
		predicates: append([]predicate.WorkspaceSyncEvent{}, wseq.predicates...),
		// clone intermediate query.
		sql:  wseq.sql.Clone(),
		path: wseq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WorkspaceSyncEvent.Query().
//		GroupBy(workspacesyncevent.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wseq *WorkspaceSyncEventQuery) GroupBy(field string, fields ...string) *WorkspaceSyncEventGroupBy {
	wseq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WorkspaceSyncEventGroupBy{build: wseq}
	grbuild.flds = &wseq.ctx.Fields
	grbuild.label = workspacesyncevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.WorkspaceSyncEvent.Query().
//		Select(workspacesyncevent.FieldCreatedAt).
//		Scan(ctx, &v)
func (wseq *WorkspaceSyncEventQuery) Select(fields ...string) *WorkspaceSyncEventSelect {
	wseq.ctx.Fields = append(wseq.ctx.Fields, fields...)
	sbuild := &WorkspaceSyncEventSelect{WorkspaceSyncEventQuery: wseq}
	sbuild.label = workspacesyncevent.Label
	sbuild.flds, sbuild.scan = &wseq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WorkspaceSyncEventSelect configured with the given aggregations.
func (wseq *WorkspaceSyncEventQuery) Aggregate(fns ...AggregateFunc) *WorkspaceSyncEventSelect {
	return wseq.Select().Aggregate(fns...)
}

func (wseq *WorkspaceSyncEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wseq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wseq); err != nil {
				return err
			}
		}
	}
	for _, f := range wseq.ctx.Fields {
		if !workspacesyncevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wseq.path != nil {
		prev, err := wseq.path(ctx)
		if err != nil {
			return err
		}
		wseq.sql = prev
	}
	return nil
}

func (wseq *WorkspaceSyncEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WorkspaceSyncEvent, error) {
	var (
		nodes = []*WorkspaceSyncEvent{}
		_spec = wseq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WorkspaceSyncEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WorkspaceSyncEvent{config: wseq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wseq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wseq *WorkspaceSyncEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wseq.querySpec()
	_spec.Node.Columns = wseq.ctx.Fields
	if len(wseq.ctx.Fields) > 0 {
		_spec.Unique = wseq.ctx.Unique != nil && *wseq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wseq.driver, _spec)
}

func (wseq *WorkspaceSyncEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(workspacesyncevent.Table, workspacesyncevent.Columns, sqlgraph.NewFieldSpec(workspacesyncevent.FieldID, field.TypeString))
	_spec.From = wseq.sql
	if unique := wseq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wseq.path != nil {
		_spec.Unique = true
	}
	if fields := wseq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, workspacesyncevent.FieldID)
		for i := range fields {
			if fields[i] != workspacesyncevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wseq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wseq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wseq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wseq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wseq *WorkspaceSyncEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wseq.driver.Dialect())
	t1 := builder.Table(workspacesyncevent.Table)
	columns := wseq.ctx.Fields
	if len(columns) == 0 {
		columns = workspacesyncevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wseq.sql != nil {
		selector = wseq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wseq.ctx.Unique != nil && *wseq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wseq.predicates {
		p(selector)
	}
	for _, p := range wseq.order {
		p(selector)
	}
	if offset := wseq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wseq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WorkspaceSyncEventGroupBy is the group-by builder for WorkspaceSyncEvent entities.
type WorkspaceSyncEventGroupBy struct {
	selector
	build *WorkspaceSyncEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wsegb *WorkspaceSyncEventGroupBy) Aggregate(fns ...AggregateFunc) *WorkspaceSyncEventGroupBy {
	wsegb.fns = append(wsegb.fns, fns...)
	return wsegb
}

// Scan applies the selector query and scans the result into the given value.
func (wsegb *WorkspaceSyncEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wsegb.build.ctx, "GroupBy")
	if err := wsegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkspaceSyncEventQuery, *WorkspaceSyncEventGroupBy](ctx, wsegb.build, wsegb, wsegb.build.inters, v)
}

func (wsegb *WorkspaceSyncEventGroupBy) sqlScan(ctx context.Context, root *WorkspaceSyncEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wsegb.fns))
	for _, fn := range wsegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wsegb.flds)+len(wsegb.fns))
		for _, f := range *wsegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wsegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wsegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WorkspaceSyncEventSelect is the builder for selecting fields of WorkspaceSyncEvent entities.
type WorkspaceSyncEventSelect struct {
	*WorkspaceSyncEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wses *WorkspaceSyncEventSelect) Aggregate(fns ...AggregateFunc) *WorkspaceSyncEventSelect {
	wses.fns = append(wses.fns, fns...)
	return wses
}

// Scan applies the selector query and scans the result into the given value.
func (wses *WorkspaceSyncEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wses.ctx, "Select")
	if err := wses.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkspaceSyncEventQuery, *WorkspaceSyncEventSelect](ctx, wses.WorkspaceSyncEventQuery, wses, wses.inters, v)
}

func (wses *WorkspaceSyncEventSelect) sqlScan(ctx context.Context, root *WorkspaceSyncEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wses.fns))
	for _, fn := range wses.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wses.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wses.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
