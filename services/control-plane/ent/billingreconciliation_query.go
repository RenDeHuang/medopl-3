// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/control-plane/ent/billingreconciliation"
	"opl-cloud/services/control-plane/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// BillingReconciliationQuery is the builder for querying BillingReconciliation entities.
type BillingReconciliationQuery struct {
	config
	ctx        *QueryContext
	order      []billingreconciliation.OrderOption
	inters     []Interceptor
	predicates []predicate.BillingReconciliation
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BillingReconciliationQuery builder.
func (brq *BillingReconciliationQuery) Where(ps ...predicate.BillingReconciliation) *BillingReconciliationQuery {
	brq.predicates = append(brq.predicates, ps...)
	return brq
}

// Limit the number of records to be returned by this query.
func (brq *BillingReconciliationQuery) Limit(limit int) *BillingReconciliationQuery {
	brq.ctx.Limit = &limit
	return brq
}

// Offset to start from.
func (brq *BillingReconciliationQuery) Offset(offset int) *BillingReconciliationQuery {
	brq.ctx.Offset = &offset
	return brq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (brq *BillingReconciliationQuery) Unique(unique bool) *BillingReconciliationQuery {
	brq.ctx.Unique = &unique
	return brq
}

// Order specifies how the records should be ordered.
func (brq *BillingReconciliationQuery) Order(o ...billingreconciliation.OrderOption) *BillingReconciliationQuery {
	brq.order = append(brq.order, o...)
	return brq
}

// First returns the first BillingReconciliation entity from the query.
// Returns a *NotFoundError when no BillingReconciliation was found.
func (brq *BillingReconciliationQuery) First(ctx context.Context) (*BillingReconciliation, error) {
	nodes, err := brq.Limit(1).All(setContextOp(ctx, brq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{billingreconciliation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (brq *BillingReconciliationQuery) FirstX(ctx context.Context) *BillingReconciliation {
	node, err := brq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BillingReconciliation ID from the query.
// Returns a *NotFoundError when no BillingReconciliation ID was found.
func (brq *BillingReconciliationQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = brq.Limit(1).IDs(setContextOp(ctx, brq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{billingreconciliation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (brq *BillingReconciliationQuery) FirstIDX(ctx context.Context) string {
	id, err := brq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BillingReconciliation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BillingReconciliation entity is found.
// Returns a *NotFoundError when no BillingReconciliation entities are found.
func (brq *BillingReconciliationQuery) Only(ctx context.Context) (*BillingReconciliation, error) {
	nodes, err := brq.Limit(2).All(setContextOp(ctx, brq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{billingreconciliation.Label}
	default:
		return nil, &NotSingularError{billingreconciliation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (brq *BillingReconciliationQuery) OnlyX(ctx context.Context) *BillingReconciliation {
	node, err := brq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BillingReconciliation ID in the query.
// Returns a *NotSingularError when more than one BillingReconciliation ID is found.
// Returns a *NotFoundError when no entities are found.
func (brq *BillingReconciliationQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = brq.Limit(2).IDs(setContextOp(ctx, brq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{billingreconciliation.Label}
	default:
		err = &NotSingularError{billingreconciliation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (brq *BillingReconciliationQuery) OnlyIDX(ctx context.Context) string {
	id, err := brq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BillingReconciliations.
func (brq *BillingReconciliationQuery) All(ctx context.Context) ([]*BillingReconciliation, error) {
	ctx = setContextOp(ctx, brq.ctx, "All")
	if err := brq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BillingReconciliation, *BillingReconciliationQuery]()
	return withInterceptors[[]*BillingReconciliation](ctx, brq, qr, brq.inters)
}

// AllX is like All, but panics if an error occurs.
func (brq *BillingReconciliationQuery) AllX(ctx context.Context) []*BillingReconciliation {
	nodes, err := brq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BillingReconciliation IDs.
func (brq *BillingReconciliationQuery) IDs(ctx context.Context) (ids []string, err error) {
	if brq.ctx.Unique == nil && brq.path != nil {
		brq.Unique(true)
	}
	ctx = setContextOp(ctx, brq.ctx, "IDs")
	if err = brq.Select(billingreconciliation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (brq *BillingReconciliationQuery) IDsX(ctx context.Context) []string {
	ids, err := brq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (brq *BillingReconciliationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, brq.ctx, "Count")
	if err := brq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, brq, querierCount[*BillingReconciliationQuery](), brq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (brq *BillingReconciliationQuery) CountX(ctx context.Context) int {
	count, err := brq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (brq *BillingReconciliationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, brq.ctx, "Exist")
	switch _, err := brq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (brq *BillingReconciliationQuery) ExistX(ctx context.Context) bool {
	exist, err := brq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BillingReconciliationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (brq *BillingReconciliationQuery) Clone() *BillingReconciliationQuery {
	if brq == nil {
		return nil
	}
	return &BillingReconciliationQuery{
		config:     brq.config,
		ctx:        brq.ctx.Clone(),
		order:      append([]billingreconciliation.OrderOption{}, brq.order...),
		inters:     append([]Interceptor{}, brq.inters...),
		predicates: append([]predicate.BillingReconciliation{}, brq.predicates...),
		// clone intermediate query.
		sql:  brq.sql.Clone(),
		path: brq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BillingReconciliation.Query().
//		GroupBy(billingreconciliation.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (brq *BillingReconciliationQuery) GroupBy(field string, fields ...string) *BillingReconciliationGroupBy {
	brq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BillingReconciliationGroupBy{build: brq}
	grbuild.flds = &brq.ctx.Fields
	grbuild.label = billingreconciliation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.BillingReconciliation.Query().
//		Select(billingreconciliation.FieldCreatedAt).
//		Scan(ctx, &v)
func (brq *BillingReconciliationQuery) Select(fields ...string) *BillingReconciliationSelect {
	brq.ctx.Fields = append(brq.ctx.Fields, fields...)
	sbuild := &BillingReconciliationSelect{BillingReconciliationQuery: brq}
	sbuild.label = billingreconciliation.Label
	sbuild.flds, sbuild.scan = &brq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BillingReconciliationSelect configured with the given aggregations.
func (brq *BillingReconciliationQuery) Aggregate(fns ...AggregateFunc) *BillingReconciliationSelect {
	return brq.Select().Aggregate(fns...)
}

func (brq *BillingReconciliationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range brq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, brq); err != nil {
				return err
			}
		}
	}
	for _, f := range brq.ctx.Fields {
		if !billingreconciliation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if brq.path != nil {
		prev, err := brq.path(ctx)
		if err != nil {
			return err
		}
		brq.sql = prev
	}
	return nil
}

func (brq *BillingReconciliationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BillingReconciliation, error) {
	var (
		nodes = []*BillingReconciliation{}
		_spec = brq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BillingReconciliation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BillingReconciliation{config: brq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, brq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (brq *BillingReconciliationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := brq.querySpec()
	_spec.Node.Columns = brq.ctx.Fields
	if len(brq.ctx.Fields) > 0 {
		_spec.Unique = brq.ctx.Unique != nil && *brq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, brq.driver, _spec)
}

func (brq *BillingReconciliationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(billingreconciliation.Table, billingreconciliation.Columns, sqlgraph.NewFieldSpec(billingreconciliation.FieldID, field.TypeString))
	_spec.From = brq.sql
	if unique := brq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if brq.path != nil {
		_spec.Unique = true
	}
	if fields := brq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, billingreconciliation.FieldID)
		for i := range fields {
			if fields[i] != billingreconciliation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := brq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := brq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := brq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := brq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (brq *BillingReconciliationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(brq.driver.Dialect())
	t1 := builder.Table(billingreconciliation.Table)
	columns := brq.ctx.Fields
	if len(columns) == 0 {
		columns = billingreconciliation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if brq.sql != nil {
		selector = brq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if brq.ctx.Unique != nil && *brq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range brq.predicates {
		p(selector)
	}
	for _, p := range brq.order {
		p(selector)
	}
	if offset := brq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := brq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BillingReconciliationGroupBy is the group-by builder for BillingReconciliation entities.
type BillingReconciliationGroupBy struct {
	selector
	build *BillingReconciliationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (brgb *BillingReconciliationGroupBy) Aggregate(fns ...AggregateFunc) *BillingReconciliationGroupBy {
	brgb.fns = append(brgb.fns, fns...)
	return brgb
}

// Scan applies the selector query and scans the result into the given value.
func (brgb *BillingReconciliationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, brgb.build.ctx, "GroupBy")
	if err := brgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BillingReconciliationQuery, *BillingReconciliationGroupBy](ctx, brgb.build, brgb, brgb.build.inters, v)
}

func (brgb *BillingReconciliationGroupBy) sqlScan(ctx context.Context, root *BillingReconciliationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(brgb.fns))
	for _, fn := range brgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*brgb.flds)+len(brgb.fns))
		for _, f := range *brgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*brgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := brgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BillingReconciliationSelect is the builder for selecting fields of BillingReconciliation entities.
type BillingReconciliationSelect struct {
	*BillingReconciliationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (brs *BillingReconciliationSelect) Aggregate(fns ...AggregateFunc) *BillingReconciliationSelect {
	brs.fns = append(brs.fns, fns...)
	return brs
}

// Scan applies the selector query and scans the result into the given value.
func (brs *BillingReconciliationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, brs.ctx, "Select")
	if err := brs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BillingReconciliationQuery, *BillingReconciliationSelect](ctx, brs.BillingReconciliationQuery, brs, brs.inters, v)
}

func (brs *BillingReconciliationSelect) sqlScan(ctx context.Context, root *BillingReconciliationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(brs.fns))
	for _, fn := range brs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*brs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := brs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
