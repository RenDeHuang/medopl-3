// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/control-plane/ent/predicate"
	"opl-cloud/services/control-plane/ent/projecttasksynchead"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ProjectTaskSyncHeadQuery is the builder for querying ProjectTaskSyncHead entities.
type ProjectTaskSyncHeadQuery struct {
	config
	ctx        *QueryContext
	order      []projecttasksynchead.OrderOption
	inters     []Interceptor
	predicates []predicate.ProjectTaskSyncHead
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProjectTaskSyncHeadQuery builder.
func (ptshq *ProjectTaskSyncHeadQuery) Where(ps ...predicate.ProjectTaskSyncHead) *ProjectTaskSyncHeadQuery {
	ptshq.predicates = append(ptshq.predicates, ps...)
	return ptshq
}

// Limit the number of records to be returned by this query.
func (ptshq *ProjectTaskSyncHeadQuery) Limit(limit int) *ProjectTaskSyncHeadQuery {
	ptshq.ctx.Limit = &limit
	return ptshq
}

// Offset to start from.
func (ptshq *ProjectTaskSyncHeadQuery) Offset(offset int) *ProjectTaskSyncHeadQuery {
	ptshq.ctx.Offset = &offset
	return ptshq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ptshq *ProjectTaskSyncHeadQuery) Unique(unique bool) *ProjectTaskSyncHeadQuery {
	ptshq.ctx.Unique = &unique
	return ptshq
}

// Order specifies how the records should be ordered.
func (ptshq *ProjectTaskSyncHeadQuery) Order(o ...projecttasksynchead.OrderOption) *ProjectTaskSyncHeadQuery {
	ptshq.order = append(ptshq.order, o...)
	return ptshq
}

// First returns the first ProjectTaskSyncHead entity from the query.
// Returns a *NotFoundError when no ProjectTaskSyncHead was found.
func (ptshq *ProjectTaskSyncHeadQuery) First(ctx context.Context) (*ProjectTaskSyncHead, error) {
	nodes, err := ptshq.Limit(1).All(setContextOp(ctx, ptshq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{projecttasksynchead.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ptshq *ProjectTaskSyncHeadQuery) FirstX(ctx context.Context) *ProjectTaskSyncHead {
	node, err := ptshq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProjectTaskSyncHead ID from the query.
// Returns a *NotFoundError when no ProjectTaskSyncHead ID was found.
func (ptshq *ProjectTaskSyncHeadQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ptshq.Limit(1).IDs(setContextOp(ctx, ptshq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{projecttasksynchead.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ptshq *ProjectTaskSyncHeadQuery) FirstIDX(ctx context.Context) string {
	id, err := ptshq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProjectTaskSyncHead entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProjectTaskSyncHead entity is found.
// Returns a *NotFoundError when no ProjectTaskSyncHead entities are found.
func (ptshq *ProjectTaskSyncHeadQuery) Only(ctx context.Context) (*ProjectTaskSyncHead, error) {
	nodes, err := ptshq.Limit(2).All(setContextOp(ctx, ptshq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{projecttasksynchead.Label}
	default:
		return nil, &NotSingularError{projecttasksynchead.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ptshq *ProjectTaskSyncHeadQuery) OnlyX(ctx context.Context) *ProjectTaskSyncHead {
	node, err := ptshq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProjectTaskSyncHead ID in the query.
// Returns a *NotSingularError when more than one ProjectTaskSyncHead ID is found.
// Returns a *NotFoundError when no entities are found.
func (ptshq *ProjectTaskSyncHeadQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ptshq.Limit(2).IDs(setContextOp(ctx, ptshq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{projecttasksynchead.Label}
	default:
		err = &NotSingularError{projecttasksynchead.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ptshq *ProjectTaskSyncHeadQuery) OnlyIDX(ctx context.Context) string {
	id, err := ptshq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProjectTaskSyncHeads.
func (ptshq *ProjectTaskSyncHeadQuery) All(ctx context.Context) ([]*ProjectTaskSyncHead, error) {
	ctx = setContextOp(ctx, ptshq.ctx, "All")
	if err := ptshq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProjectTaskSyncHead, *ProjectTaskSyncHeadQuery]()
	return withInterceptors[[]*ProjectTaskSyncHead](ctx, ptshq, qr, ptshq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ptshq *ProjectTaskSyncHeadQuery) AllX(ctx context.Context) []*ProjectTaskSyncHead {
	nodes, err := ptshq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProjectTaskSyncHead IDs.
func (ptshq *ProjectTaskSyncHeadQuery) IDs(ctx context.Context) (ids []string, err error) {
	if ptshq.ctx.Unique == nil && ptshq.path != nil {
		ptshq.Unique(true)
	}
	ctx = setContextOp(ctx, ptshq.ctx, "IDs")
	if err = ptshq.Select(projecttasksynchead.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ptshq *ProjectTaskSyncHeadQuery) IDsX(ctx context.Context) []string {
	ids, err := ptshq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ptshq *ProjectTaskSyncHeadQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ptshq.ctx, "Count")
	if err := ptshq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ptshq, querierCount[*ProjectTaskSyncHeadQuery](), ptshq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ptshq *ProjectTaskSyncHeadQuery) CountX(ctx context.Context) int {
	count, err := ptshq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ptshq *ProjectTaskSyncHeadQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ptshq.ctx, "Exist")
	switch _, err := ptshq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ptshq *ProjectTaskSyncHeadQuery) ExistX(ctx context.Context) bool {
	exist, err := ptshq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProjectTaskSyncHeadQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ptshq *ProjectTaskSyncHeadQuery) Clone() *ProjectTaskSyncHeadQuery {
	if ptshq == nil {
		return nil
	}
	return &ProjectTaskSyncHeadQuery{
		config:     ptshq.config,
		ctx:        ptshq.ctx.Clone(),
		order:      append([]projecttasksynchead.OrderOption{}, ptshq.order...),
		inters:     append([]Interceptor{}, ptshq.inters...),
		predicates: append([]predicate.ProjectTaskSyncHead{}, ptshq.predicates...),
		// clone intermediate query.
		sql:  ptshq.sql.Clone(),
		path: ptshq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProjectTaskSyncHead.Query().
//		GroupBy(projecttasksynchead.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ptshq *ProjectTaskSyncHeadQuery) GroupBy(field string, fields ...string) *ProjectTaskSyncHeadGroupBy {
	ptshq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProjectTaskSyncHeadGroupBy{build: ptshq}
	grbuild.flds = &ptshq.ctx.Fields
	grbuild.label = projecttasksynchead.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ProjectTaskSyncHead.Query().
//		Select(projecttasksynchead.FieldCreatedAt).
//		Scan(ctx, &v)
func (ptshq *ProjectTaskSyncHeadQuery) Select(fields ...string) *ProjectTaskSyncHeadSelect {
	ptshq.ctx.Fields = append(ptshq.ctx.Fields, fields...)
	sbuild := &ProjectTaskSyncHeadSelect{ProjectTaskSyncHeadQuery: ptshq}
	sbuild.label = projecttasksynchead.Label
	sbuild.flds, sbuild.scan = &ptshq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProjectTaskSyncHeadSelect configured with the given aggregations.
func (ptshq *ProjectTaskSyncHeadQuery) Aggregate(fns ...AggregateFunc) *ProjectTaskSyncHeadSelect {
	return ptshq.Select().Aggregate(fns...)
}

func (ptshq *ProjectTaskSyncHeadQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ptshq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ptshq); err != nil {
				return err
			}
		}
	}
	for _, f := range ptshq.ctx.Fields {
		if !projecttasksynchead.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ptshq.path != nil {
		prev, err := ptshq.path(ctx)
		if err != nil {
			return err
		}
		ptshq.sql = prev
	}
	return nil
}

func (ptshq *ProjectTaskSyncHeadQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProjectTaskSyncHead, error) {
	var (
		nodes = []*ProjectTaskSyncHead{}
		_spec = ptshq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProjectTaskSyncHead).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProjectTaskSyncHead{config: ptshq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ptshq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ptshq *ProjectTaskSyncHeadQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ptshq.querySpec()
	_spec.Node.Columns = ptshq.ctx.Fields
	if len(ptshq.ctx.Fields) > 0 {
		_spec.Unique = ptshq.ctx.Unique != nil && *ptshq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ptshq.driver, _spec)
}

func (ptshq *ProjectTaskSyncHeadQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(projecttasksynchead.Table, projecttasksynchead.Columns, sqlgraph.NewFieldSpec(projecttasksynchead.FieldID, field.TypeString))
	_spec.From = ptshq.sql
	if unique := ptshq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ptshq.path != nil {
		_spec.Unique = true
	}
	if fields := ptshq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, projecttasksynchead.FieldID)
		for i := range fields {
			if fields[i] != projecttasksynchead.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ptshq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ptshq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ptshq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ptshq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ptshq *ProjectTaskSyncHeadQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ptshq.driver.Dialect())
	t1 := builder.Table(projecttasksynchead.Table)
	columns := ptshq.ctx.Fields
	if len(columns) == 0 {
		columns = projecttasksynchead.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ptshq.sql != nil {
		selector = ptshq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ptshq.ctx.Unique != nil && *ptshq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ptshq.predicates {
		p(selector)
	}
	for _, p := range ptshq.order {
		p(selector)
	}
	if offset := ptshq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ptshq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProjectTaskSyncHeadGroupBy is the group-by builder for ProjectTaskSyncHead entities.
type ProjectTaskSyncHeadGroupBy struct {
	selector
	build *ProjectTaskSyncHeadQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ptshgb *ProjectTaskSyncHeadGroupBy) Aggregate(fns ...AggregateFunc) *ProjectTaskSyncHeadGroupBy {
	ptshgb.fns = append(ptshgb.fns, fns...)
	return ptshgb
}

// Scan applies the selector query and scans the result into the given value.
func (ptshgb *ProjectTaskSyncHeadGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ptshgb.build.ctx, "GroupBy")
	if err := ptshgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProjectTaskSyncHeadQuery, *ProjectTaskSyncHeadGroupBy](ctx, ptshgb.build, ptshgb, ptshgb.build.inters, v)
}

func (ptshgb *ProjectTaskSyncHeadGroupBy) sqlScan(ctx context.Context, root *ProjectTaskSyncHeadQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ptshgb.fns))
	for _, fn := range ptshgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ptshgb.flds)+len(ptshgb.fns))
		for _, f := range *ptshgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ptshgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ptshgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProjectTaskSyncHeadSelect is the builder for selecting fields of ProjectTaskSyncHead entities.
type ProjectTaskSyncHeadSelect struct {
	*ProjectTaskSyncHeadQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ptshs *ProjectTaskSyncHeadSelect) Aggregate(fns ...AggregateFunc) *ProjectTaskSyncHeadSelect {
	ptshs.fns = append(ptshs.fns, fns...)
	return ptshs
}

// Scan applies the selector query and scans the result into the given value.
func (ptshs *ProjectTaskSyncHeadSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ptshs.ctx, "Select")
	if err := ptshs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProjectTaskSyncHeadQuery, *ProjectTaskSyncHeadSelect](ctx, ptshs.ProjectTaskSyncHeadQuery, ptshs, ptshs.inters, v)
}

func (ptshs *ProjectTaskSyncHeadSelect) sqlScan(ctx context.Context, root *ProjectTaskSyncHeadQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ptshs.fns))
	for _, fn := range ptshs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ptshs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ptshs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
