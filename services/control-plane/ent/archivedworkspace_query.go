// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/control-plane/ent/archivedworkspace"
	"opl-cloud/services/control-plane/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ArchivedWorkspaceQuery is the builder for querying ArchivedWorkspace entities.
type ArchivedWorkspaceQuery struct {
	config
	ctx        *QueryContext
	order      []archivedworkspace.OrderOption
	inters     []Interceptor
	predicates []predicate.ArchivedWorkspace
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ArchivedWorkspaceQuery builder.
func (awq *ArchivedWorkspaceQuery) Where(ps ...predicate.ArchivedWorkspace) *ArchivedWorkspaceQuery {
	awq.predicates = append(awq.predicates, ps...)
	return awq
}

// Limit the number of records to be returned by this query.
func (awq *ArchivedWorkspaceQuery) Limit(limit int) *ArchivedWorkspaceQuery {
	awq.ctx.Limit = &limit
	return awq
}

// Offset to start from.
func (awq *ArchivedWorkspaceQuery) Offset(offset int) *ArchivedWorkspaceQuery {
	awq.ctx.Offset = &offset
	return awq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (awq *ArchivedWorkspaceQuery) Unique(unique bool) *ArchivedWorkspaceQuery {
	awq.ctx.Unique = &unique
	return awq
}

// Order specifies how the records should be ordered.
func (awq *ArchivedWorkspaceQuery) Order(o ...archivedworkspace.OrderOption) *ArchivedWorkspaceQuery {
	awq.order = append(awq.order, o...)
	return awq
}

// First returns the first ArchivedWorkspace entity from the query.
// Returns a *NotFoundError when no ArchivedWorkspace was found.
func (awq *ArchivedWorkspaceQuery) First(ctx context.Context) (*ArchivedWorkspace, error) {
	nodes, err := awq.Limit(1).All(setContextOp(ctx, awq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{archivedworkspace.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (awq *ArchivedWorkspaceQuery) FirstX(ctx context.Context) *ArchivedWorkspace {
	node, err := awq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ArchivedWorkspace ID from the query.
// Returns a *NotFoundError when no ArchivedWorkspace ID was found.
func (awq *ArchivedWorkspaceQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = awq.Limit(1).IDs(setContextOp(ctx, awq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{archivedworkspace.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (awq *ArchivedWorkspaceQuery) FirstIDX(ctx context.Context) string {
	id, err := awq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ArchivedWorkspace entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ArchivedWorkspace entity is found.
// Returns a *NotFoundError when no ArchivedWorkspace entities are found.
func (awq *ArchivedWorkspaceQuery) Only(ctx context.Context) (*ArchivedWorkspace, error) {
	nodes, err := awq.Limit(2).All(setContextOp(ctx, awq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{archivedworkspace.Label}
	default:
		return nil, &NotSingularError{archivedworkspace.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (awq *ArchivedWorkspaceQuery) OnlyX(ctx context.Context) *ArchivedWorkspace {
	node, err := awq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ArchivedWorkspace ID in the query.
// Returns a *NotSingularError when more than one ArchivedWorkspace ID is found.
// Returns a *NotFoundError when no entities are found.
func (awq *ArchivedWorkspaceQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = awq.Limit(2).IDs(setContextOp(ctx, awq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{archivedworkspace.Label}
	default:
		err = &NotSingularError{archivedworkspace.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (awq *ArchivedWorkspaceQuery) OnlyIDX(ctx context.Context) string {
	id, err := awq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ArchivedWorkspaces.
func (awq *ArchivedWorkspaceQuery) All(ctx context.Context) ([]*ArchivedWorkspace, error) {
	ctx = setContextOp(ctx, awq.ctx, "All")
	if err := awq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ArchivedWorkspace, *ArchivedWorkspaceQuery]()
	return withInterceptors[[]*ArchivedWorkspace](ctx, awq, qr, awq.inters)
}

// AllX is like All, but panics if an error occurs.
func (awq *ArchivedWorkspaceQuery) AllX(ctx context.Context) []*ArchivedWorkspace {
	nodes, err := awq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ArchivedWorkspace IDs.
func (awq *ArchivedWorkspaceQuery) IDs(ctx context.Context) (ids []string, err error) {
	if awq.ctx.Unique == nil && awq.path != nil {
		awq.Unique(true)
	}
	ctx = setContextOp(ctx, awq.ctx, "IDs")
	if err = awq.Select(archivedworkspace.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (awq *ArchivedWorkspaceQuery) IDsX(ctx context.Context) []string {
	ids, err := awq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (awq *ArchivedWorkspaceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, awq.ctx, "Count")
	if err := awq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, awq, querierCount[*ArchivedWorkspaceQuery](), awq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (awq *ArchivedWorkspaceQuery) CountX(ctx context.Context) int {
	count, err := awq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (awq *ArchivedWorkspaceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, awq.ctx, "Exist")
	switch _, err := awq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (awq *ArchivedWorkspaceQuery) ExistX(ctx context.Context) bool {
	exist, err := awq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ArchivedWorkspaceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (awq *ArchivedWorkspaceQuery) Clone() *ArchivedWorkspaceQuery {
	if awq == nil {
		return nil
	}
	return &ArchivedWorkspaceQuery{
		config:     awq.config,
		ctx:        awq.ctx.Clone(),
		order:      append([]archivedworkspace.OrderOption{}, awq.order...),
		inters:     append([]Interceptor{}, awq.inters...),
		predicates: append([]predicate.ArchivedWorkspace{}, awq.predicates...),
		// clone intermediate query.
		sql:  awq.sql.Clone(),
		path: awq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ArchivedWorkspace.Query().
//		GroupBy(archivedworkspace.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (awq *ArchivedWorkspaceQuery) GroupBy(field string, fields ...string) *ArchivedWorkspaceGroupBy {
	awq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ArchivedWorkspaceGroupBy{build: awq}
	grbuild.flds = &awq.ctx.Fields
	grbuild.label = archivedworkspace.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ArchivedWorkspace.Query().
//		Select(archivedworkspace.FieldCreatedAt).
//		Scan(ctx, &v)
func (awq *ArchivedWorkspaceQuery) Select(fields ...string) *ArchivedWorkspaceSelect {
	awq.ctx.Fields = append(awq.ctx.Fields, fields...)
	sbuild := &ArchivedWorkspaceSelect{ArchivedWorkspaceQuery: awq}
	sbuild.label = archivedworkspace.Label
	sbuild.flds, sbuild.scan = &awq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ArchivedWorkspaceSelect configured with the given aggregations.
func (awq *ArchivedWorkspaceQuery) Aggregate(fns ...AggregateFunc) *ArchivedWorkspaceSelect {
	return awq.Select().Aggregate(fns...)
}

func (awq *ArchivedWorkspaceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range awq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, awq); err != nil {
				return err
			}
		}
	}
	for _, f := range awq.ctx.Fields {
		if !archivedworkspace.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if awq.path != nil {
		prev, err := awq.path(ctx)
		if err != nil {
			return err
		}
		awq.sql = prev
	}
	return nil
}

func (awq *ArchivedWorkspaceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ArchivedWorkspace, error) {
	var (
		nodes = []*ArchivedWorkspace{}
		_spec = awq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ArchivedWorkspace).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ArchivedWorkspace{config: awq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, awq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (awq *ArchivedWorkspaceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := awq.querySpec()
	_spec.Node.Columns = awq.ctx.Fields
	if len(awq.ctx.Fields) > 0 {
		_spec.Unique = awq.ctx.Unique != nil && *awq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, awq.driver, _spec)
}

func (awq *ArchivedWorkspaceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(archivedworkspace.Table, archivedworkspace.Columns, sqlgraph.NewFieldSpec(archivedworkspace.FieldID, field.TypeString))
	_spec.From = awq.sql
	if unique := awq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if awq.path != nil {
		_spec.Unique = true
	}
	if fields := awq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, archivedworkspace.FieldID)
		for i := range fields {
			if fields[i] != archivedworkspace.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := awq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := awq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := awq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := awq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (awq *ArchivedWorkspaceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(awq.driver.Dialect())
	t1 := builder.Table(archivedworkspace.Table)
	columns := awq.ctx.Fields
	if len(columns) == 0 {
		columns = archivedworkspace.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if awq.sql != nil {
		selector = awq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if awq.ctx.Unique != nil && *awq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range awq.predicates {
		p(selector)
	}
	for _, p := range awq.order {
		p(selector)
	}
	if offset := awq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := awq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ArchivedWorkspaceGroupBy is the group-by builder for ArchivedWorkspace entities.
type ArchivedWorkspaceGroupBy struct {
	selector
	build *ArchivedWorkspaceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (awgb *ArchivedWorkspaceGroupBy) Aggregate(fns ...AggregateFunc) *ArchivedWorkspaceGroupBy {
	awgb.fns = append(awgb.fns, fns...)
	return awgb
}

// Scan applies the selector query and scans the result into the given value.
func (awgb *ArchivedWorkspaceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, awgb.build.ctx, "GroupBy")
	if err := awgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ArchivedWorkspaceQuery, *ArchivedWorkspaceGroupBy](ctx, awgb.build, awgb, awgb.build.inters, v)
}

func (awgb *ArchivedWorkspaceGroupBy) sqlScan(ctx context.Context, root *ArchivedWorkspaceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(awgb.fns))
	for _, fn := range awgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*awgb.flds)+len(awgb.fns))
		for _, f := range *awgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*awgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := awgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ArchivedWorkspaceSelect is the builder for selecting fields of ArchivedWorkspace entities.
type ArchivedWorkspaceSelect struct {
	*ArchivedWorkspaceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (aws *ArchivedWorkspaceSelect) Aggregate(fns ...AggregateFunc) *ArchivedWorkspaceSelect {
	aws.fns = append(aws.fns, fns...)
	return aws
}

// Scan applies the selector query and scans the result into the given value.
func (aws *ArchivedWorkspaceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aws.ctx, "Select")
	if err := aws.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ArchivedWorkspaceQuery, *ArchivedWorkspaceSelect](ctx, aws.ArchivedWorkspaceQuery, aws, aws.inters, v)
}

func (aws *ArchivedWorkspaceSelect) sqlScan(ctx context.Context, root *ArchivedWorkspaceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(aws.fns))
	for _, fn := range aws.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*aws.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aws.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
