// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/control-plane/ent/archivejob"
	"opl-cloud/services/control-plane/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ArchiveJobQuery is the builder for querying ArchiveJob entities.
type ArchiveJobQuery struct {
	config
	ctx        *QueryContext
	order      []archivejob.OrderOption
	inters     []Interceptor
	predicates []predicate.ArchiveJob
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ArchiveJobQuery builder.
func (ajq *ArchiveJobQuery) Where(ps ...predicate.ArchiveJob) *ArchiveJobQuery {
	ajq.predicates = append(ajq.predicates, ps...)
	return ajq
}

// Limit the number of records to be returned by this query.
func (ajq *ArchiveJobQuery) Limit(limit int) *ArchiveJobQuery {
	ajq.ctx.Limit = &limit
	return ajq
}

// Offset to start from.
func (ajq *ArchiveJobQuery) Offset(offset int) *ArchiveJobQuery {
	ajq.ctx.Offset = &offset
	return ajq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ajq *ArchiveJobQuery) Unique(unique bool) *ArchiveJobQuery {
	ajq.ctx.Unique = &unique
	return ajq
}

// Order specifies how the records should be ordered.
func (ajq *ArchiveJobQuery) Order(o ...archivejob.OrderOption) *ArchiveJobQuery {
	ajq.order = append(ajq.order, o...)
	return ajq
}

// First returns the first ArchiveJob entity from the query.
// Returns a *NotFoundError when no ArchiveJob was found.
func (ajq *ArchiveJobQuery) First(ctx context.Context) (*ArchiveJob, error) {
	nodes, err := ajq.Limit(1).All(setContextOp(ctx, ajq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{archivejob.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ajq *ArchiveJobQuery) FirstX(ctx context.Context) *ArchiveJob {
	node, err := ajq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ArchiveJob ID from the query.
// Returns a *NotFoundError when no ArchiveJob ID was found.
func (ajq *ArchiveJobQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ajq.Limit(1).IDs(setContextOp(ctx, ajq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{archivejob.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ajq *ArchiveJobQuery) FirstIDX(ctx context.Context) string {
	id, err := ajq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ArchiveJob entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ArchiveJob entity is found.
// Returns a *NotFoundError when no ArchiveJob entities are found.
func (ajq *ArchiveJobQuery) Only(ctx context.Context) (*ArchiveJob, error) {
	nodes, err := ajq.Limit(2).All(setContextOp(ctx, ajq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{archivejob.Label}
	default:
		return nil, &NotSingularError{archivejob.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ajq *ArchiveJobQuery) OnlyX(ctx context.Context) *ArchiveJob {
	node, err := ajq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ArchiveJob ID in the query.
// Returns a *NotSingularError when more than one ArchiveJob ID is found.
// Returns a *NotFoundError when no entities are found.
func (ajq *ArchiveJobQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ajq.Limit(2).IDs(setContextOp(ctx, ajq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{archivejob.Label}
	default:
		err = &NotSingularError{archivejob.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ajq *ArchiveJobQuery) OnlyIDX(ctx context.Context) string {
	id, err := ajq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ArchiveJobs.
func (ajq *ArchiveJobQuery) All(ctx context.Context) ([]*ArchiveJob, error) {
	ctx = setContextOp(ctx, ajq.ctx, "All")
	if err := ajq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ArchiveJob, *ArchiveJobQuery]()
	return withInterceptors[[]*ArchiveJob](ctx, ajq, qr, ajq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ajq *ArchiveJobQuery) AllX(ctx context.Context) []*ArchiveJob {
	nodes, err := ajq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ArchiveJob IDs.
func (ajq *ArchiveJobQuery) IDs(ctx context.Context) (ids []string, err error) {
	if ajq.ctx.Unique == nil && ajq.path != nil {
		ajq.Unique(true)
	}
	ctx = setContextOp(ctx, ajq.ctx, "IDs")
	if err = ajq.Select(archivejob.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ajq *ArchiveJobQuery) IDsX(ctx context.Context) []string {
	ids, err := ajq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ajq *ArchiveJobQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ajq.ctx, "Count")
	if err := ajq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ajq, querierCount[*ArchiveJobQuery](), ajq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ajq *ArchiveJobQuery) CountX(ctx context.Context) int {
	count, err := ajq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ajq *ArchiveJobQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ajq.ctx, "Exist")
	switch _, err := ajq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ajq *ArchiveJobQuery) ExistX(ctx context.Context) bool {
	exist, err := ajq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ArchiveJobQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ajq *ArchiveJobQuery) Clone() *ArchiveJobQuery {
	if ajq == nil {
		return nil
	}
	return &ArchiveJobQuery{
		config:     ajq.config,
		ctx:        ajq.ctx.Clone(),
		order:      append([]archivejob.OrderOption{}, ajq.order...),
		inters:     append([]Interceptor{}, ajq.inters...),
		predicates: append([]predicate.ArchiveJob{}, ajq.predicates...),
		// clone intermediate query.
		sql:  ajq.sql.Clone(),
		path: ajq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ArchiveJob.Query().
//		GroupBy(archivejob.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ajq *ArchiveJobQuery) GroupBy(field string, fields ...string) *ArchiveJobGroupBy {
	ajq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ArchiveJobGroupBy{build: ajq}
	grbuild.flds = &ajq.ctx.Fields
	grbuild.label = archivejob.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ArchiveJob.Query().
//		Select(archivejob.FieldCreatedAt).
//		Scan(ctx, &v)
func (ajq *ArchiveJobQuery) Select(fields ...string) *ArchiveJobSelect {
	ajq.ctx.Fields = append(ajq.ctx.Fields, fields...)
	sbuild := &ArchiveJobSelect{ArchiveJobQuery: ajq}
	sbuild.label = archivejob.Label
	sbuild.flds, sbuild.scan = &ajq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ArchiveJobSelect configured with the given aggregations.
func (ajq *ArchiveJobQuery) Aggregate(fns ...AggregateFunc) *ArchiveJobSelect {
	return ajq.Select().Aggregate(fns...)
}

func (ajq *ArchiveJobQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ajq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ajq); err != nil {
				return err
			}
		}
	}
	for _, f := range ajq.ctx.Fields {
		if !archivejob.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ajq.path != nil {
		prev, err := ajq.path(ctx)
		if err != nil {
			return err
		}
		ajq.sql = prev
	}
	return nil
}

func (ajq *ArchiveJobQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ArchiveJob, error) {
	var (
		nodes = []*ArchiveJob{}
		_spec = ajq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ArchiveJob).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ArchiveJob{config: ajq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ajq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ajq *ArchiveJobQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ajq.querySpec()
	_spec.Node.Columns = ajq.ctx.Fields
	if len(ajq.ctx.Fields) > 0 {
		_spec.Unique = ajq.ctx.Unique != nil && *ajq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ajq.driver, _spec)
}

func (ajq *ArchiveJobQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(archivejob.Table, archivejob.Columns, sqlgraph.NewFieldSpec(archivejob.FieldID, field.TypeString))
	_spec.From = ajq.sql
	if unique := ajq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ajq.path != nil {
		_spec.Unique = true
	}
	if fields := ajq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, archivejob.FieldID)
		for i := range fields {
			if fields[i] != archivejob.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ajq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ajq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ajq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ajq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ajq *ArchiveJobQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ajq.driver.Dialect())
	t1 := builder.Table(archivejob.Table)
	columns := ajq.ctx.Fields
	if len(columns) == 0 {
		columns = archivejob.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ajq.sql != nil {
		selector = ajq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ajq.ctx.Unique != nil && *ajq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ajq.predicates {
		p(selector)
	}
	for _, p := range ajq.order {
		p(selector)
	}
	if offset := ajq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ajq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ArchiveJobGroupBy is the group-by builder for ArchiveJob entities.
type ArchiveJobGroupBy struct {
	selector
	build *ArchiveJobQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ajgb *ArchiveJobGroupBy) Aggregate(fns ...AggregateFunc) *ArchiveJobGroupBy {
	ajgb.fns = append(ajgb.fns, fns...)
	return ajgb
}

// Scan applies the selector query and scans the result into the given value.
func (ajgb *ArchiveJobGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ajgb.build.ctx, "GroupBy")
	if err := ajgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ArchiveJobQuery, *ArchiveJobGroupBy](ctx, ajgb.build, ajgb, ajgb.build.inters, v)
}

func (ajgb *ArchiveJobGroupBy) sqlScan(ctx context.Context, root *ArchiveJobQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ajgb.fns))
	for _, fn := range ajgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ajgb.flds)+len(ajgb.fns))
		for _, f := range *ajgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ajgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ajgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ArchiveJobSelect is the builder for selecting fields of ArchiveJob entities.
type ArchiveJobSelect struct {
	*ArchiveJobQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ajs *ArchiveJobSelect) Aggregate(fns ...AggregateFunc) *ArchiveJobSelect {
	ajs.fns = append(ajs.fns, fns...)
	return ajs
}

// Scan applies the selector query and scans the result into the given value.
func (ajs *ArchiveJobSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ajs.ctx, "Select")
	if err := ajs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ArchiveJobQuery, *ArchiveJobSelect](ctx, ajs.ArchiveJobQuery, ajs, ajs.inters, v)
}

func (ajs *ArchiveJobSelect) sqlScan(ctx context.Context, root *ArchiveJobQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ajs.fns))
	for _, fn := range ajs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ajs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ajs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
