// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/fabric/ent/environmenttemplate"
	"opl-cloud/services/fabric/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// EnvironmentTemplateQuery is the builder for querying EnvironmentTemplate entities.
type EnvironmentTemplateQuery struct {
	config
	ctx        *QueryContext
	order      []environmenttemplate.OrderOption
	inters     []Interceptor
	predicates []predicate.EnvironmentTemplate
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EnvironmentTemplateQuery builder.
func (etq *EnvironmentTemplateQuery) Where(ps ...predicate.EnvironmentTemplate) *EnvironmentTemplateQuery {
	etq.predicates = append(etq.predicates, ps...)
	return etq
}

// Limit the number of records to be returned by this query.
func (etq *EnvironmentTemplateQuery) Limit(limit int) *EnvironmentTemplateQuery {
	etq.ctx.Limit = &limit
	return etq
}

// Offset to start from.
func (etq *EnvironmentTemplateQuery) Offset(offset int) *EnvironmentTemplateQuery {
	etq.ctx.Offset = &offset
	return etq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (etq *EnvironmentTemplateQuery) Unique(unique bool) *EnvironmentTemplateQuery {
	etq.ctx.Unique = &unique
	return etq
}

// Order specifies how the records should be ordered.
func (etq *EnvironmentTemplateQuery) Order(o ...environmenttemplate.OrderOption) *EnvironmentTemplateQuery {
	etq.order = append(etq.order, o...)
	return etq
}

// First returns the first EnvironmentTemplate entity from the query.
// Returns a *NotFoundError when no EnvironmentTemplate was found.
func (etq *EnvironmentTemplateQuery) First(ctx context.Context) (*EnvironmentTemplate, error) {
	nodes, err := etq.Limit(1).All(setContextOp(ctx, etq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{environmenttemplate.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (etq *EnvironmentTemplateQuery) FirstX(ctx context.Context) *EnvironmentTemplate {
	node, err := etq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EnvironmentTemplate ID from the query.
// Returns a *NotFoundError when no EnvironmentTemplate ID was found.
func (etq *EnvironmentTemplateQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = etq.Limit(1).IDs(setContextOp(ctx, etq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{environmenttemplate.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (etq *EnvironmentTemplateQuery) FirstIDX(ctx context.Context) string {
	id, err := etq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EnvironmentTemplate entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EnvironmentTemplate entity is found.
// Returns a *NotFoundError when no EnvironmentTemplate entities are found.
func (etq *EnvironmentTemplateQuery) Only(ctx context.Context) (*EnvironmentTemplate, error) {
	nodes, err := etq.Limit(2).All(setContextOp(ctx, etq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{environmenttemplate.Label}
	default:
		return nil, &NotSingularError{environmenttemplate.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (etq *EnvironmentTemplateQuery) OnlyX(ctx context.Context) *EnvironmentTemplate {
	node, err := etq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EnvironmentTemplate ID in the query.
// Returns a *NotSingularError when more than one EnvironmentTemplate ID is found.
// Returns a *NotFoundError when no entities are found.
func (etq *EnvironmentTemplateQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = etq.Limit(2).IDs(setContextOp(ctx, etq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{environmenttemplate.Label}
	default:
		err = &NotSingularError{environmenttemplate.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (etq *EnvironmentTemplateQuery) OnlyIDX(ctx context.Context) string {
	id, err := etq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EnvironmentTemplates.
func (etq *EnvironmentTemplateQuery) All(ctx context.Context) ([]*EnvironmentTemplate, error) {
	ctx = setContextOp(ctx, etq.ctx, "All")
	if err := etq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EnvironmentTemplate, *EnvironmentTemplateQuery]()
	return withInterceptors[[]*EnvironmentTemplate](ctx, etq, qr, etq.inters)
}

// AllX is like All, but panics if an error occurs.
func (etq *EnvironmentTemplateQuery) AllX(ctx context.Context) []*EnvironmentTemplate {
	nodes, err := etq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EnvironmentTemplate IDs.
func (etq *EnvironmentTemplateQuery) IDs(ctx context.Context) (ids []string, err error) {
	if etq.ctx.Unique == nil && etq.path != nil {
		etq.Unique(true)
	}
	ctx = setContextOp(ctx, etq.ctx, "IDs")
	if err = etq.Select(environmenttemplate.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (etq *EnvironmentTemplateQuery) IDsX(ctx context.Context) []string {
	ids, err := etq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (etq *EnvironmentTemplateQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, etq.ctx, "Count")
	if err := etq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, etq, querierCount[*EnvironmentTemplateQuery](), etq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (etq *EnvironmentTemplateQuery) CountX(ctx context.Context) int {
	count, err := etq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (etq *EnvironmentTemplateQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, etq.ctx, "Exist")
	switch _, err := etq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (etq *EnvironmentTemplateQuery) ExistX(ctx context.Context) bool {
	exist, err := etq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EnvironmentTemplateQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (etq *EnvironmentTemplateQuery) Clone() *EnvironmentTemplateQuery {
	if etq == nil {
		return nil
	}
	return &EnvironmentTemplateQuery{
		config:     etq.config,
		ctx:        etq.ctx.Clone(),
		order:      append([]environmenttemplate.OrderOption{}, etq.order...),
		inters:     append([]Interceptor{}, etq.inters...),
		predicates: append([]predicate.EnvironmentTemplate{}, etq.predicates...),
		// clone intermediate query.
		sql:  etq.sql.Clone(),
		path: etq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TemplateID string `json:"template_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EnvironmentTemplate.Query().
//		GroupBy(environmenttemplate.FieldTemplateID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (etq *EnvironmentTemplateQuery) GroupBy(field string, fields ...string) *EnvironmentTemplateGroupBy {
	etq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EnvironmentTemplateGroupBy{build: etq}
	grbuild.flds = &etq.ctx.Fields
	grbuild.label = environmenttemplate.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TemplateID string `json:"template_id,omitempty"`
//	}
//
//	client.EnvironmentTemplate.Query().
//		Select(environmenttemplate.FieldTemplateID).
//		Scan(ctx, &v)
func (etq *EnvironmentTemplateQuery) Select(fields ...string) *EnvironmentTemplateSelect {
	etq.ctx.Fields = append(etq.ctx.Fields, fields...)
	sbuild := &EnvironmentTemplateSelect{EnvironmentTemplateQuery: etq}
	sbuild.label = environmenttemplate.Label
	sbuild.flds, sbuild.scan = &etq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EnvironmentTemplateSelect configured with the given aggregations.
func (etq *EnvironmentTemplateQuery) Aggregate(fns ...AggregateFunc) *EnvironmentTemplateSelect {
	return etq.Select().Aggregate(fns...)
}

func (etq *EnvironmentTemplateQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range etq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, etq); err != nil {
				return err
			}
		}
	}
	for _, f := range etq.ctx.Fields {
		if !environmenttemplate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if etq.path != nil {
		prev, err := etq.path(ctx)
		if err != nil {
			return err
		}
		etq.sql = prev
	}
	return nil
}

func (etq *EnvironmentTemplateQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EnvironmentTemplate, error) {
	var (
		nodes = []*EnvironmentTemplate{}
		_spec = etq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EnvironmentTemplate).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EnvironmentTemplate{config: etq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, etq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (etq *EnvironmentTemplateQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := etq.querySpec()
	_spec.Node.Columns = etq.ctx.Fields
	if len(etq.ctx.Fields) > 0 {
		_spec.Unique = etq.ctx.Unique != nil && *etq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, etq.driver, _spec)
}

func (etq *EnvironmentTemplateQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(environmenttemplate.Table, environmenttemplate.Columns, sqlgraph.NewFieldSpec(environmenttemplate.FieldID, field.TypeString))
	_spec.From = etq.sql
	if unique := etq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if etq.path != nil {
		_spec.Unique = true
	}
	if fields := etq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, environmenttemplate.FieldID)
		for i := range fields {
			if fields[i] != environmenttemplate.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := etq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := etq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := etq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := etq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (etq *EnvironmentTemplateQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(etq.driver.Dialect())
	t1 := builder.Table(environmenttemplate.Table)
	columns := etq.ctx.Fields
	if len(columns) == 0 {
		columns = environmenttemplate.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if etq.sql != nil {
		selector = etq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if etq.ctx.Unique != nil && *etq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range etq.predicates {
		p(selector)
	}
	for _, p := range etq.order {
		p(selector)
	}
	if offset := etq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := etq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EnvironmentTemplateGroupBy is the group-by builder for EnvironmentTemplate entities.
type EnvironmentTemplateGroupBy struct {
	selector
	build *EnvironmentTemplateQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (etgb *EnvironmentTemplateGroupBy) Aggregate(fns ...AggregateFunc) *EnvironmentTemplateGroupBy {
	etgb.fns = append(etgb.fns, fns...)
	return etgb
}

// Scan applies the selector query and scans the result into the given value.
func (etgb *EnvironmentTemplateGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, etgb.build.ctx, "GroupBy")
	if err := etgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EnvironmentTemplateQuery, *EnvironmentTemplateGroupBy](ctx, etgb.build, etgb, etgb.build.inters, v)
}

func (etgb *EnvironmentTemplateGroupBy) sqlScan(ctx context.Context, root *EnvironmentTemplateQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(etgb.fns))
	for _, fn := range etgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*etgb.flds)+len(etgb.fns))
		for _, f := range *etgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*etgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := etgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EnvironmentTemplateSelect is the builder for selecting fields of EnvironmentTemplate entities.
type EnvironmentTemplateSelect struct {
	*EnvironmentTemplateQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ets *EnvironmentTemplateSelect) Aggregate(fns ...AggregateFunc) *EnvironmentTemplateSelect {
	ets.fns = append(ets.fns, fns...)
	return ets
}

// Scan applies the selector query and scans the result into the given value.
func (ets *EnvironmentTemplateSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ets.ctx, "Select")
	if err := ets.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EnvironmentTemplateQuery, *EnvironmentTemplateSelect](ctx, ets.EnvironmentTemplateQuery, ets, ets.inters, v)
}

func (ets *EnvironmentTemplateSelect) sqlScan(ctx context.Context, root *EnvironmentTemplateQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ets.fns))
	for _, fn := range ets.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ets.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ets.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
